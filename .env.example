# ==========================================
# Required API Keys
# ==========================================

# Google AI (Gemini) API Key - Required for all AI operations
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY=your_gemini_api_key_here

# Redis (via Vercel Marketplace) - For document metadata storage
# Automatically created when you connect Redis database in Vercel Storage
# Variable name: KV_REDIS_URL (with Redis icon in Vercel dashboard)
# See: https://vercel.com/docs/storage
KV_REDIS_URL=your_redis_connection_url_here

# Vercel Blob Storage - For permanent file storage (downloads)
# Automatically created when you connect Blob storage in Vercel Storage
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# Google Custom Search API - For Research Agent
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CUSTOM_SEARCH_API_KEY=your_custom_search_api_key
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=your_search_engine_id

# ==========================================
# Optional Features
# ==========================================

# Jina.ai Reader API Key (Optional) - For URL ingestion
# Get your free API key from: https://jina.ai/reader/
# Without key: 20 requests/min (tracked by IP)
# With key: 500 requests/min (10M free tokens)
# JINA_API_KEY=your_jina_api_key_here

# ==========================================
# Security Settings (Optional)
# ==========================================

# Confirmation token for /api/corpus/clear-all (prevents accidental deletion)
# Default: DELETE_ALL_DOCUMENTS (if not set)
# Set a custom secure random token to use this endpoint
# CLEAR_ALL_TOKEN=your_secure_random_token_here

# Enable migration endpoint (only enable when needed, then disable)
# Default: false (disabled for security)
# Set to 'true' to run /api/migrate endpoint once, then remove or set to false
# ENABLE_MIGRATION=false

# CORS - Allowed origin for API requests
# Default: http://localhost:3000 (development)
# Production: Set to your deployed domain (e.g., https://trs-mocha.vercel.app)
# ALLOWED_ORIGIN=http://localhost:3000
